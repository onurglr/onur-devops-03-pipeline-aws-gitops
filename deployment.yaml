apiVersion: apps/v1
kind: Deployment
metadata:
  name: devops-03-pipeline-cd-onur-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: devops-03-pipeline-cd-onur
  template:
    metadata:
      labels:
        app: devops-03-pipeline-cd-onur
    spec:
      containers:
        - name: devops-03-pipeline-cd-onur
          image: onurguler18/devops-03-pipeline-aws-onur:1.0.16
          resources:
            limits:
              memory: "256Mi"
              cpu: "500m"
          ports:
            - containerPort: 8080   # Pod içindeki .jar uygulamanın portu